import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krisurya.zitro.restaurant',
  appName: 'Zitro Restaurant Partner',
  webDir: 'dist/apps/zitro-restaurant/browser',
};

export default config;
