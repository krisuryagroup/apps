# ZITRO Restaurant Partner Portal — Manual / Real-Time Test Scenarios

> **Purpose:** a human-executable QA checklist for `zitro-restaurant`, run against a live
> dev/staging server — not the automated Vitest/Playwright suite (that's `RS-TEST-001`/`002`,
> still `[ ]`). Written by reading the actually-shipped code (`app.routes.ts`, the standalone
> component `imports` arrays and inline templates under `features/`, `BusinessApiService`,
> and the `zitro-api` controller for the AI import endpoint) rather than transcribing
> `RESTAURANT-TASKS.md`'s original spec. Several scenarios below exist specifically because
> the shipped component does noticeably less than its task card promised — those are called
> out inline and rolled up in §17.
>
> **Status reference:** `RESTAURANT-STATUS.md` — RS-000 through RS-016 are `[x]` done;
> RS-017 (push notifications) and RS-018 (Firebase Hosting deploy) are `[ ]`, see §17.

---

## Live Execution Log — 2026-08-19

This section is the actual, tracked record of running this document against a real local
stack (`zitro-api` + local Postgres `zitro-dev`, `zitro-restaurant` on `:4201`) — not a
re-statement of the checklist above. Follows the same format as
`ADMIN-TEST-SCENARIOS.md`/`SUPERADMIN-TEST-SCENARIOS.md`'s logs. Updated as execution
proceeded.

**Test data used:**

| What                             | Value                                                                                                                                                                  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Business (pre-existing seed)     | EFC Pizza, id `13858247-ab0c-4628-8e73-69ee63e04c62`                                                                                                                   | `business_type: restaurant`. Used for all business-related tests.                                                                                                                                                                                                                                                                                                                                                                                  |
| Owner login                      | EFC Owner / `+917890000000` / `Partner@123`                                                                                                                            | Existing business-user row, password reset to a known value for testing. **Updated 2026-08-22** (was `RestaurantTest@2026`) — this same password was also set on every other test business-owner account used across this session, so one credential works everywhere: Sanjeev Kumar / `8998989898` (Yellow Bowl - Gulshan Bellina), `9111222333` / `9222233344` / `9998887771` (the map/lat-lng test businesses — see `ADMIN-TEST-SCENARIOS.md`). |
| New staff created (RS-T-1702)    | Ramesh Kumar / `+917890000099` / `StaffTest@2026` / role `manager`                                                                                                     | Created via the Add Staff form after the `phoneNumber` fix; left in place as real test data (not cleaned up — a manager account is legitimate ongoing test fixture).                                                                                                                                                                                                                                                                               |
| Test rating created (direct SQL) | 4★, "Pizza was great but delivery took a while."                                                                                                                       | Inserted directly (`ratings` table, `target_type='restaurant'`) since no ratings existed anywhere in the dev DB — needed to exercise RS-013's non-empty render + reply path. Left in place.                                                                                                                                                                                                                                                        |
| Test payout created (direct SQL) | ₹12,500 gross / ₹11,187.50 net, status `paid`, `PAYOUT-TEST-001`                                                                                                       | Inserted directly (`restaurant_payouts` table) — same reasoning, no payouts existed to test RS-014's non-empty render. Left in place.                                                                                                                                                                                                                                                                                                              |
| Test category created            | "Desserts" (accidental duplicate "Pizzas" cleaned up via direct SQL `DELETE` — soft-delete via UI didn't remove it from the admin-inclusive list query, see fix table) |                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Profile description edit         | Appended "(updated via live test)" to EFC Pizza's description, saved twice                                                                                             | Left in place — cosmetic, harmless.                                                                                                                                                                                                                                                                                                                                                                                                                |

**Issues found and fixed:**

| #   | Scenario(s)        | Issue                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status              | Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------------------------------------- |
| 1   | RS-T-101           | Login always 400'd — frontend sent `{ phone, password }`, backend requires `phoneNumber`.                                                                                                                                                                                                                                                                                                                                                                | **FIXED**           | `apps` commit `7652a6a` — `login()` now sends `{ phoneNumber, password }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2   | RS-T-103           | Deactivated business account collapsed into generic `INVALID_CREDENTIALS` — same bug pattern already fixed for AdminLogin (`be1e927`). Query filtered `IsActive` upfront instead of checking it as a distinct step.                                                                                                                                                                                                                                      | **FIXED**           | `zitro-api` commit `ba93dc5` — query by phone only, verify password, then check `IsActive` separately, returning `ACCOUNT_DEACTIVATED`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 3   | RS-T-203/205       | Partner application form 400'd — `Slug` was non-nullable in `CreateBusinessApplicationRequest`/`Command` and the frontend never sent one.                                                                                                                                                                                                                                                                                                                | **FIXED**           | `zitro-api` commit `47e8756` — `Slug` made optional; handler auto-derives + disambiguates a slug from `Name` when omitted, matching the existing Tags pattern.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 4   | RS-T-201/203       | `submitApplication()` request body used `town`/`password` instead of the real `address: { town }`/`ownerPassword` fields.                                                                                                                                                                                                                                                                                                                                | **FIXED**           | `apps` commit `9e12a01`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | RS-T-204/305/801   | ~~"Confirmed bug" per this doc's original text~~ — **already fixed before this session**, in commit `05afe8a` (docs commit that also added `RouterLink` to both onboarding components and the menu component). Verified live: all three links/buttons navigate correctly. Doc language below is now corrected.                                                                                                                                           | N/A                 | Pre-existing fix, not from this session — see RS-T-1901's updated summary below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 6   | RS-T-102, RS-T-205 | Login and Apply error handlers read `err?.error?.error` (human message) instead of `err?.error?.errorCode` (machine code) — same recurring anti-pattern already fixed for AdminLogin (`084cc1e`). Found in login + apply's CONFLICT check.                                                                                                                                                                                                               | **FIXED**           | `apps` commit `9e12a01` — both read `errorCode`; apply's check also widened to `DUPLICATE_PHONE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |     | DUPLICATE_SLUG`(the real codes) instead of a fictional`CONFLICT`. |
| 7   | RS-T-601/602       | Orders list showed empty despite dashboard reporting a non-zero pending count — `listOrders()` typed as a bare array, but the API returns `{ orders, totalCount, page, pageSize }`. `newCollection[Symbol.iterator] is not a function`.                                                                                                                                                                                                                  | **FIXED**           | `apps` commit `15792ac` — unwrap `res.orders`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 8   | RS-T-701/704       | Order Detail items/timeline blank: `OrderItemDto` field names (`productName`/`unitPrice`/`quantity`/`variationName`) didn't match the real API (`name`/`price`/`qty`/`selectedVariationLabel`); the status timeline was never queried/returned by `GetOrderDetailHandler` at all (pure wiring gap).                                                                                                                                                      | **FIXED**           | `zitro-api` commit `c318321` — query + return `OrderStatusTimeline` rows. `apps` commit `ff9245e` — corrected item field names in the template.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 9   | RS-T-801, RS-T-802 | Menu screen showed "No items found" for a business with a real menu — `BusinessPortalCatalogController` had **no `GET`** for products/categories at all (POST/PUT/DELETE only), so the initial load 404'd silently.                                                                                                                                                                                                                                      | **FIXED**           | `zitro-api` commit `c24a21f` — added `GET .../products` and `GET .../categories`, backed by new `ListBusinessProductsQuery`/`ListBusinessCategoriesQuery`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 10  | RS-T-803           | New category rendered with no name (just the delete "✕") — `createCategory`'s response is `{ id }` only, but the component optimistically appended it as a full `MenuCategoryDto`.                                                                                                                                                                                                                                                                       | **FIXED**           | `apps` commit `df5edc4` — reload the full list after create instead of appending the partial object (same fix shape as Brands/Tags/Admins earlier in the session).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 11  | RS-T-1904          | **Highest-impact bug found this phase.** Every screen's content was completely invisible below the 768px mobile breakpoint — `restaurant-layout.component.ts`'s `:host { display: flex }` (row) was never switched to `column` in the mobile media query, squeezing `.main-content` to zero width and off-screen.                                                                                                                                        | **FIXED**           | `apps` commit `ef62541` — mobile media query now also sets `:host { flex-direction: column }`. Also swept and fixed the systemic fictional-CSS-token bug (`--zitro-color-*` → real `--zitro-*` tokens) across every component in this app, including 7 files' inline `styles:` blocks a `.scss`-only grep sweep had initially missed.                                                                                                                                                                                                                                                                                                                                                                    |
| 12  | RS-T-1701/1702     | Staff screen: `StaffDto.phone` didn't match the real API field `phoneNumber` (Phone column blank for every row); create form sent the wrong field name too.                                                                                                                                                                                                                                                                                              | **FIXED**           | `apps` commit `d5d4cfc`. Verified end-to-end: list shows EFC Owner's real phone; created "Ramesh Kumar" (manager) returns 201 and appears correctly after reload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 13  | RS-T-1401          | Ratings screen was blank even after a real 4★ rating existed in the DB — same paginated-wrapper bug as #7 (`{ items, totalCount, ... }` vs. bare-array typing). Also `RatingDto`'s field names (`rating`/`comment`/`reply`/`targetType`) didn't match the real API (`ratingValue`/`reviewText`/`replyText`; `targetType` is never projected server-side). `replyToRating()` posted `{ reply }` but the backend expects `{ replyText, repliedByUserId }`. | **FIXED**           | `apps` commit `0c9c1ed` — unwrapped `.items`, renamed DTO fields, dropped the non-existent target-type badge, added a `currentUserId` computed (decoded from the JWT `sub` claim) and fixed the reply request body. Added a missing `@empty` state (an empty list previously rendered nothing at all). Verified end-to-end: rating card renders with stars + review; reply POST returns 200 and appears immediately.                                                                                                                                                                                                                                                                                     |
| 14  | RS-T-1501          | Payouts screen blank even with a real payout row in the DB — same paginated-wrapper bug as #7/#13.                                                                                                                                                                                                                                                                                                                                                       | **FIXED**           | `apps` commit `0c9c1ed` — unwrapped `.items` (backend `PayoutDto` field names already matched). Verified: payout row renders with correct period/gross/net/status.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 15  | RS-T-1603          | **Most severe bug found this phase.** `UpdateProfile` returns `204 No Content` by design (see the endpoint's own doc comment), but `updateProfile()` was typed `Observable<BusinessProfileDto>` and `save()`'s `next` handler did `profile.set(p)` with the undefined body — **every successful save nulled out the whole profile signal**, collapsing the entire form (the PUT itself always succeeded and persisted correctly; only the UI broke).     | **FIXED**           | `apps` commit `f53efd0` — `updateProfile()` retyped `Observable<void>`; `save()` now merges the saved fields into the existing `profile()` signal instead of expecting a returned object. Verified: edited Description, saved twice — form and status banner stay intact, "Saved" message appears.                                                                                                                                                                                                                                                                                                                                                                                                       |
| 16  | RS-T-1203          | `saveAdjust()` (Inventory) had no `error:` callback at all — a rejected adjustment failed completely silently, panel stayed open with zero feedback.                                                                                                                                                                                                                                                                                                     | **FIXED**           | `apps` commit `c423388` — added an `adjustError` signal + error text, matching the delivery-zone form's existing pattern. **Not live-tested against a real `INSUFFICIENT_STOCK` response** — inventory is grocery-only (`zitro-api/CLAUDE.md` §26) and EFC Pizza (the only local business with a working login) is `business_type: restaurant` with an intentionally empty inventory table; no grocery (Tularam Kirana Store) login exists in seed data. Verified by source + build only.                                                                                                                                                                                                                |
| 17  | RS-T-1303          | ~~`zone-fee-base` testid on the wrong element (`<label>` not `<input>`)~~ — **already fixed before this session**, testid confirmed on the `<input>` in current source.                                                                                                                                                                                                                                                                                  | N/A                 | Pre-existing fix, not from this session.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 18  | RS-T-1703          | Staff table was add-only, display-only after that — `updateStaff()` existed on the service and was fully wired for a PUT call, but nothing in `RestaurantStaffComponent`'s template ever called it. `BusinessPortalController.UpdateUser` already accepted `Name`/`Role`/`IsActive`/`NewPassword` in one unified endpoint.                                                                                                                               | **FIXED**           | `apps` commit `4ca100e` (2026-08-19, follow-up pass) — added Edit/Deactivate/Reset Password row actions, reusing the existing add-staff form panel for edit. Also fixed the same 204-No-Content response-typing bug already fixed for Profile/Payouts/Ratings: `updateStaff()` was typed `Observable<StaffDto>` but `UpdateUser` returns `NoContent()`. Verified end-to-end: edited Ramesh Kumar's role manager→staff (persisted after reload), deactivated (curl confirms `ACCOUNT_DEACTIVATED` on login attempt), reset password while deactivated (curl confirms the new password is accepted independently of the active-status check), reactivated (curl confirms 200 login with the new password). |
| 19  | RS-T-703           | Order Detail's Customer section rendered `order()!.customerPhone` directly with no masking.                                                                                                                                                                                                                                                                                                                                                              | **FIXED**           | `apps` commit `e43fdda` (2026-08-19, follow-up pass) — added `maskPhone()`, showing only the last 4 digits. Verified: a real order's phone renders as `••••••••9268`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 20  | RS-T-1502          | `PayoutDto` was missing `commissionAmount`/`orderCount`/`payoutReference` even though `GetPayoutsHandler` already returns them — frontend only mapped 7 of the backend record's 10 fields.                                                                                                                                                                                                                                                               | **PARTIALLY FIXED** | `apps` commit `e43fdda` — added the three missing fields and matching table columns. Verified: the test payout row now shows Orders: 42, Commission: ₹1250, Reference: PAYOUT-TEST-001. **Bank account display still doesn't exist anywhere in the API** (unchanged, larger gap — no bank-account field exists on the business/profile side either, see RS-T-1502's original note).                                                                                                                                                                                                                                                                                                                      |

**Confirmed, NOT fixed (documented gaps — verified still accurate against current source):**

- **RS-T-104**: Login still doesn't branch on `onboardingStatus` — every login lands on `/dashboard` regardless of approval state. Confirmed gap, unchanged.
- **RS-T-401**: `/onboarding` is still a 2-line stub, not a KYC form. No PAN/bank/document-upload fields anywhere. Confirmed gap, unchanged.
- **RS-T-502**: No revenue chart on the dashboard despite `weekRevenue` being fetched. Confirmed gap, unchanged.
- **RS-T-702**: Order Detail charges section still only shows "Total" — no packaging/platform/GST/delivery/coupon breakdown rendered. Confirmed gap, unchanged (the timeline half of this section, RS-T-704, is now fixed — see #8 above; the phone-masking half of the same section, RS-T-703, is now fixed too — see #19).
- **RS-T-808/809**: Menu item form still has only 4 fields + availability — no description, image, variants, or extra flags; food type limited to veg/non-veg. Confirmed gap, unchanged.
- **RS-T-904–910 (§9b)**: Not exercised this phase — flag-on AI import requires `ANTHROPIC_API_KEY` + a SuperAdmin feature-flag toggle not set up in this local environment. §9a (flag-off fallback) was implicitly exercised via the menu screen fix (#9 above) and works correctly.
- **§10/§11 (RS-009/RS-010)**: Confirmed still not implemented — no bulk-spreadsheet or clone-from-branch routes/components exist.
- **RS-T-1304/1306**: Delivery zone form still has no per-km/surge fields and no active/inactive toggle. Confirmed gap, unchanged.
- **RS-T-1305**: No map/radius zone-drawing UI — every zone create still 400s on missing `PolygonCoords`, surfaced with a clear message (fixed earlier this session as RS-T-1903) rather than failing silently. Flagged as a follow-up task (map vs. radius-fallback is a product decision, not a quick fix).
- **RS-T-1502**: Commission amount now displays (fix #20) — bank account still doesn't exist anywhere in the API. Partial gap remains.
- **RS-T-1601/1602**: Profile screen still has zero `data-testid` attributes and only 5 of ~9 spec'd fields (no address/hours/min-order/delivery-fee/pure-veg/images). Confirmed gap, unchanged.
- **RS-T-1604**: `updateProfile()` still sends all 5 fields on every save, not a diff. Confirmed gap, unchanged (unrelated to fix #15 — that fix corrected the response-handling, not the request shape).
- **RS-T-1704**: Staff screen still has no role-based route guard (edit/deactivate/reset-password UI itself is now fixed — see #18). Confirmed gap, unchanged.
- **RS-T-1902**: No role-differentiated route guards anywhere in `app.routes.ts` beyond authentication. Confirmed gap, unchanged.

**RouterLink audit (RS-T-1901) re-run for this session:**

Re-scanned every component under `features/` and `layout/` for `routerLink` usage vs. the
component's own `imports` array. **Zero missing-`RouterLink`-import instances found** — every
component that uses `routerLink` in its template imports `RouterLink`. The three instances
this doc originally flagged as "confirmed bugs" (RS-T-204, RS-T-305, RS-T-801) were already
fixed prior to this session's start, in commit `05afe8a`; their entries above (§2, §3, §8)
should be read as historical/fixed, not open bugs — the sections themselves haven't been
edited to avoid rewriting the original review, but this log is the authoritative current
status.

**Scenario results:**

- §1 Authentication (RS-T-101–108): **all pass** after fixes #1, #2, #6. RS-T-104 confirmed gap, unchanged.
- §2 Partner Application (RS-T-201–207): **all pass** after fixes #3, #4, #6. RS-T-204's "dead link" text is stale — link works (see RouterLink audit above).
- §3 Accept Invite (RS-T-301–307): **all pass**. RS-T-305's "dead link" text is stale — link works.
- §4 Onboarding/KYC (RS-T-401–403): RS-T-402/403 pass (status banner + resubmit-via-profile both work). RS-T-401 confirmed major gap, unchanged.
- §5 Dashboard (RS-T-501–505): **all pass** except RS-T-502 (confirmed gap, unchanged).
- §6 Orders Queue (RS-T-601–610): **all pass** after fix #7. RS-T-608/609 (live-refresh, auto-cancel countdown) not independently re-verified this phase — flagged in the doc as needing careful confirmation, not contradicted.
- §7 Order Detail (RS-T-701–705): **pass** after fixes #8, #19 (items + timeline render correctly; phone now masked as `••••••••1234`). RS-T-702 (charges breakdown) still a confirmed gap, unchanged.
- §8 Menu Management (RS-T-801–812): **all pass** after fixes #9, #10. RS-T-801's "dead button" text is stale — AI Menu Import button works. RS-T-804 (nested button DOM) and RS-T-808/809/812 confirmed gaps, unchanged — not independently re-verified this phase.
- §9 AI Menu Import (RS-T-901–910): §9a (flag off) verified working via the menu screen fix. §9b (flag on) not exercised — requires API key + flag setup not present locally.
- §10/§11 (RS-009/RS-010): Confirmed not implemented, unchanged.
- §12 Inventory (RS-T-1201–1204): Table correctly empty for EFC Pizza (restaurant, not grocery — inventory is grocery-only per `zitro-api/CLAUDE.md` §26; this is correct behavior, not a bug). RS-T-1203 fixed (#16) but not live-verified against a real backend rejection — no grocery test login available.
- §13 Delivery Zones (RS-T-1301–1307): List/add/delete pass. RS-T-1303's testid bug is stale — already fixed. RS-T-1304/1305/1306 confirmed gaps, unchanged; RS-T-1305 flagged as a follow-up task.
- §14 Ratings & Reviews (RS-T-1401–1404): **all pass** after fix #13, verified end-to-end with real data (list render + reply). RS-T-1404 (max-length) not independently re-verified.
- §15 Payouts (RS-T-1501–1504): **all pass** after fixes #14, #20, verified end-to-end with real data (Orders/Commission/Reference columns now populated). RS-T-1502's bank-account half remains a confirmed gap.
- §16 Profile & Settings (RS-T-1601–1605): RS-T-1603 fixed (#15) — this was the most severe bug of the session (every save silently wiped the form). RS-T-1601/1602/1604/1605 confirmed gaps, unchanged.
- §17 Staff Management (RS-T-1701–1705): **all pass except the route guard** — RS-T-1701/1702 pass (list + create, fix #12). RS-T-1703 ✓ fixed (#18) — edit/deactivate/reset-password all verified end-to-end via curl. RS-T-1704 (role-based route guard) remains a confirmed gap. RS-T-1705 unchanged (incidental, not purpose-built).
- §19 Cross-cutting: RS-T-1901 re-run, zero new findings (see audit note above — the doc's own text is stale, not the code). RS-T-1902/1903(delivery-zone-only)/1904 — RS-T-1904 was this phase's biggest fix (#11); RS-T-1902 confirmed gap. RS-T-1905/1906 spot-checked incidentally throughout (every fix above was verified via direct API/network inspection, not just UI re-render) — held in every case checked.

**FULL RUN COMPLETE — all locally-testable scenarios executed** (§9b AI-import-on and the
grocery-only Inventory-adjust-error path excluded — both require environment setup not
present locally, noted above rather than skipped silently).

**Follow-up pass — 2026-08-19 (same day, quick-wins round):** fixed 3 more issues (#18–#20)
found in the original review's gap list — Staff edit/deactivate/reset-password (RS-T-1703),
Order Detail phone masking (RS-T-703), and Payout commission/order-count/reference display
(RS-T-1502, partial). Test data: Ramesh Kumar's role changed manager→staff and password reset
to `StaffTest@2027` via the new Staff UI (both verified via curl), account left active. See
issues #18–#20 in the table above for full verification detail.

**Follow-up pass — 2026-08-21 (shared-menu smoke test, doc staleness cleanup):** no new bugs
found; this pass verified that `SharedMenuComponent` (built earlier in the same session as the
admin brand-linking/business-lat-lng work, see `ADMIN-TEST-SCENARIOS.md` entry #9) works
correctly end-to-end, and corrected several rows in §8 that had gone stale since 2026-08-19
without their inline text being updated (the log already said "AI Menu Import button works"
and "zone-fee-base testid bug is stale" back then, but RS-T-801/RS-T-1303's own row text still
described them as live bugs — now fixed, see RS-T-801/RS-T-812 above). Logged in
`admin@zitroapp.in`, reset `+917890000000` (EFC Pizza owner, `menu_mode: shared`, brand `EFC
Pizza Group`)'s password locally the same way as the admin session, on `zitro-restaurant`
(`:4203` — port differs from the `:4201` recorded in the 2026-08-19 log; ports appear to be
Nx-assigned per dev-server-start order, not fixed). Verified live: `/menu` renders
`SharedMenuComponent`'s table (not the CRUD screen) for this business; setting a price override
on "7 Up" (₹45 → ₹50) and saving persisted correctly to `branch_item_overrides` (confirmed via
direct DB query). New scenarios RS-T-813–817 added for this component's behavior (RS-T-812
itself corrected from "not implemented" to "implemented" with an accurate description — the
actual mechanism is a full screen swap on `menuMode`, not a same-screen visual distinction as
originally speculated when the row was first written).

**Follow-up pass — 2026-09-06 (onboarding gap-closing Phases 1–4 — full live test + 3 bugs
found/fixed):** this pass covers every feature added by
`apps/tasks/RESTAURANT-ONBOARDING-GAP-ANALYSIS.md`'s 4 phases: OTP identity gate + KYC document
upload (Phase 1), admin-managed payout account (Phase 2), cuisine/category/hours + cover photo +
the go-live approval gate (Phase 3), and commission-terms acceptance (Phase 4). New scenarios
RS-T-208–217 (§2), RS-T-811b (§8), and RS-T-1606–1615 (§16) were added above for this; this
section is the actual execution record.

**Environment:** local `zitro-api` (`dotnet run`, `ASPNETCORE_ENVIRONMENT=Development`) against
local Postgres `zitro-dev` + local Redis, `zitro-restaurant` on `:4203` (pre-existing dev server
reused, not started fresh). Both new Phase 3/4 migrations
(`apply-2026-09-05-onboarding-completeness.sql`, `apply-2026-09-05-commission-acceptance.sql`)
had to be applied to the local DB first — they weren't yet present. **All 4 apps'
`environment.ts` (`zitro-restaurant`, `zitro-admin`, `zitro-customer`, `zitro-superadmin`) were
pointed at `http://localhost:8080` for this session** (per the user's explicit instruction, since
testing against the deployed Render backend wouldn't exercise the local code/DB changes at all)
— **local-only, per each file's own "never commit that change" comment; left pointed at local
for the rest of this working session, not reverted.**

**Test data used:**

| What                                     | Value                                                                                                                             | Notes                                                                                                                                                                                                          |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Business (pre-existing)                  | EFC Pizza, `13858247-ab0c-4628-8e73-69ee63e04c62`, `menu_mode: shared`, approved, active                                          | Used for §16 profile scenarios (cover photo, KYC doc, commission accept) — logged in as owner `+917890000000`/`Partner@123`.                                                                                   |
| New business created via full apply flow | "Test Cuisine Restaurant 2", owner `+919000022233`/`TestPass@123`, category `qsr`, cuisines `Chinese, Mexican`, hours 10:00–23:00 | Created end-to-end through the real UI (steps 1–4 including OTP) to exercise §2's new scenarios and the go-live gate deadlock (RS-T-811b). Left in place, now `approved`/`active` with 1 category + 1 product. |
| Admin login                              | `admin@zitroapp.in`, password reset locally to `AdminTest@2026` (bcrypt hash written directly to `admins.password_hash`)          | Same reset-via-SQL pattern used in `ADMIN-TEST-SCENARIOS.md` — no known password existed for this local DB copy.                                                                                               |
| Test cover photo / KYC file              | A trivial 1×1 JPEG generated locally (`/tmp/test-cover.jpg`)                                                                      | Uploaded via curl (see below) rather than through the Browser tool's file input, which cannot attach local files — see note under "Testing method" below.                                                      |

**Testing method:** most of §2 (apply form incl. OTP) and the commission-accept button (§16)
were driven through the actual rendered UI via JS-dispatched DOM events (the Browser tool's
`computer` click/type actions require the pane to be visibly on-screen and repeatedly failed
with "could not be attributed to a frame" while the pane was backgrounded — `javascript_tool`
dispatching real `input`/`click` events against the live Angular app was used instead, which
still exercises the actual component code, not a mock). **File uploads (cover photo, KYC
documents) were tested via direct `curl` calls to the same endpoints the UI calls**, since the
Browser tool has no file-picker/upload capability — the UI's own upload wiring (`onCoverPhotoSelected`/`onDocumentFileSelected` → `FormData` → the same URL) was read from source and
confirmed to match, and the resulting state (uploaded URL, "Pending" badge) was confirmed to
render correctly in the UI after each curl-driven upload on reload.

**Issues found and fixed:**

| #   | Scenario(s)               | Issue                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status    | Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | RS-T-201, RS-T-211        | Apply form's "Next" button (all 3 step-transitions) rendered the literal text `common.next` instead of "Next" — `apply.component.ts` uses `{{ 'common.next' \| i18n }}` at 3 places, but no `common.next` key was ever defined anywhere in `en.ts` (only an unrelated `dataTable.next`, a different section entirely). `I18nService.translate()` falls back to returning the raw key when missing.                                                                                                                                                                                                                                                                                                                                                                                     | **FIXED** | `apps` — added `next: 'Next'` to the `common` section of `libs/i18n/src/defaults/en.ts`. Verified live: button now shows "Next" at all 3 steps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2   | RS-T-213, RS-T-214        | **Highest-severity bug found this pass — the entire OTP-gated apply flow was unusable in local/dev testing.** `VerifyOtpHandler`'s dev-bypass branch (`Otp.BypassForDevelopment=true`, submitted code matches `Otp.BypassOtp`) returned success without ever touching the DB — it never set `OtpSession.IsUsed = true`. `CreateBusinessApplicationHandler`'s phone-verification gate checks exactly that flag, so submission always failed with `400 PHONE_NOT_VERIFIED` even immediately after a "successful" OTP verify. Since `BypassForDevelopment` is the _only_ way to complete this flow without a real Fast2SMS integration, this made Phase 1's core feature completely untestable — and would silently break for any other future consumer that checks `OtpSessions.IsUsed`. | **FIXED** | `zitro-api` — `VerifyOtpHandler`'s bypass branch now looks up the latest unused, unexpired session for the phone and marks it used before returning, exactly matching what the real (non-bypass) path does. 1 new regression test added (`VerifyOtpHandlerTests.Handle_WhenBypassOtpProvided_ShouldStillMarkSessionUsed`) plus incidentally fixed one pre-existing failing test in the same file that asserted this exact behavior. Verified live end-to-end: full apply flow (steps 1–4, OTP, submit) now succeeds.                                                                                                                                                                                                                              |
| 3   | RS-T-811b                 | **Second-highest-severity bug — a real deadlock for brand-new independent-mode businesses.** `BusinessPortalCatalogController`/`BusinessPortalMenuImportController`'s access check required `IsActive == true` to create categories/products or use AI menu import. But `IsActive` only becomes `true` once `ApproveBusinessHandler` approves the business — and Phase 3's own go-live gate refuses to approve an independent-mode business with zero products. A new applicant had no way to ever create the product needed to pass its own approval gate. (Shared-mode businesses, like EFC Pizza, were never affected — they skip the product check entirely.)                                                                                                                      | **FIXED** | `zitro-api` — both controllers' `ValidateBusinessAccessAsync` now also allow access when the business exists and `OnboardingStatus != Approved` (never yet approved), in addition to the normal `IsActive == true` case. A business that WAS approved and later deactivated still correctly stays locked out (verified with a dedicated test). Required adding `OnboardingStatus` to the cross-module `BusinessSummary` contract DTO (`Businesses.Contracts`) since it wasn't previously exposed outside the Businesses module. 3 new integration tests added (`BusinessPortalCatalogControllerTests.cs`). Verified live end-to-end: created a category + product for the Pending test business, then Admin approval succeeded immediately after. |
| 4   | RS-T-1601, RS-T-1606–1615 | `profile.component.ts`'s entire pre-existing form already had zero `data-testid` attributes (a known, already-documented gap, RS-T-1601) — but the _new_ Phase 1–4 sections added to this same file (cover photo, KYC docs, payout, commission terms) were written the same way, with no testids either, silently extending the exact anti-pattern the codebase's own conventions call out (`apps/CLAUDE.md`: "data-testid attributes only — never CSS selectors in tests").                                                                                                                                                                                                                                                                                                           | **FIXED** | `apps` — added `data-testid` to every new element: `profile-cover-photo-input`/`-preview`, `profile-document-input-{type}`/`profile-document-status-{type}` (×4 slots), `profile-payout-account`, `profile-commission-rate`, `profile-accept-commission-btn`, `profile-commission-accepted-badge`. The pre-existing 5-field core form (RS-T-1601) was left as-is — out of scope for this pass, still a confirmed gap. Verified live: all new testids resolve correctly via `document.querySelectorAll('[data-testid]')`.                                                                                                                                                                                                                          |

**Regression testing:** `dotnet build` (0 warnings/errors) and the full `Zitro.UnitTests` /
`Zitro.IntegrationTests` suites were re-run after every backend fix above, compared against the
documented 22-unit/15-integration-failure baseline via `git stash` before/after diffing (not just
raw counts) each time. Net result across all 3 backend fixes: **zero new failures**, 1
pre-existing failure incidentally fixed as a direct consequence of fix #2
(`VerifyOtpHandlerTests.Handle_WhenCorrectOtpWithBypassDisabled_ShouldMarkSessionUsedBeforeFirebaseCall`
was asserting exactly the behavior fix #2 restored), and 4 new tests added and passing (1 for
fix #2, 3 for fix #3). Frontend: `nx run-many -t build,lint` for all 4 apps — 0 errors, only
pre-existing unrelated warnings.

**Scenario results:**

- §2 Partner Application (RS-T-201–217): **all pass** after fixes #1 and #2. RS-T-210
  (24-hours-checkbox field-hiding behavior), RS-T-216 (30-minute OTP expiry window), and RS-T-217
  (resend-OTP invalidating the prior session) were not independently re-verified — documented as
  expected-per-source, not live-confirmed, flagged for a future pass.
- §8 Menu Management (RS-T-811b): **fixed and verified** — see fix #3. The pre-existing
  RS-T-801–817 results from the 2026-08-19/08-21 passes are unaffected (that testing used
  already-approved businesses, which were never subject to this deadlock).
- §16 Profile & Settings (RS-T-1606–1615): **all directly-testable scenarios pass** after fix #4.
  RS-T-1609 (re-upload-after-review), RS-T-1610 (full admin-write-then-display round-trip), and
  RS-T-1614 (rate-change-resets-acceptance, UI-level) were not independently re-verified through
  the `zitro-restaurant` UI in this pass — RS-T-1614's underlying logic IS covered by a dedicated
  backend unit test (not a live UI check), and the other two require the `zitro-admin` side of a
  round-trip not exercised in this restaurant-portal-focused pass.

---

## 0. Before you start

**Environment**

- [ ] `zitro-api` running locally or against staging.
- [ ] `nx serve zitro-restaurant` running.
- [ ] For RS-002/RS-002b (partner application + admin invite) to work **end-to-end**, two
      manual backend steps from `zitro-api/TASK-STATUS.md` TASK-039 must already be done —
      otherwise the form submits but the follow-on step 404s/500s: - the `business_invites` table applied to Neon - `EMAIL_API_KEY` set in `zitro_config.app_secrets` (email is the only new-capability
      piece of TASK-039 — public application submission itself needs no email to work)
      Without these, you can still test the **application form's own validation** (§2) but
      not the admin-invite email path, and application-submission itself should still
      succeed even without email configured (email only fires for the invite path).
- [ ] For RS-008 (AI menu import) flag-**on** scenarios, three manual steps from TASK-038
      must be done first: the `feature_flags` table + `ai_menu_import` seed row applied to
      Neon, `ANTHROPIC_API_KEY` set in `zitro_config.app_secrets`, and a SuperAdmin has run
      `PUT /api/admin/feature-flags/ai_menu_import` with `{ "isEnabled": true }` (the flag
      ships **seeded OFF** — none of the first two steps turn it on by themselves). If you
      don't have this set up, you can still run the flag-**off** scenarios (§8) — that's the
      default state on a fresh environment and is itself worth testing.

**Test accounts needed**

- [ ] One Business JWT account with `role: owner`, `onboardingStatus: approved` — the primary
      account for most of this document (§4 onward).
- [ ] One Business JWT account with `onboardingStatus: pending` or `rejected` (for §1, §3).
- [ ] One `manager`-role and one `staff`-role account on the same business, if you want to
      exercise the role-based restrictions referenced in RS-016's acceptance criteria (§16 —
      note the gap called out there: these restrictions don't currently exist to test).
- [ ] A live order on the linked business (create one from `zitro-customer` or seed data) for
      §5/§6.

**Conventions used below**

- `[data-testid="..."]` — query the element by this attribute in dev tools or Playwright.
- Where no `data-testid` exists, the scenario says so explicitly and gives the `id`/selector
  to use instead — this document found many more of these gaps than usual; don't assume a
  missing testid is your mistake, see §17 for the full list.
- **Dual-mode:** every scenario should be run at both a desktop viewport and a mobile
  viewport (this app is also shipped as an Android APK via Capacitor) per
  `RESTAURANT-TASKS.md`'s own instruction — the sidebar's mobile breakpoint is 768px
  (`layout/restaurant-layout.component.ts`).
- **Known routing gap to remember throughout:** login does **not** currently branch on
  `onboardingStatus` (see RS-T-104) — every successful login goes to `/dashboard`, full stop.
  Keep this in mind when a scenario below says "should redirect to onboarding" — that
  redirect doesn't exist yet; the scenario documents the gap rather than an assumption.

---

## 1. Authentication & Access (RS-001)

**Backend:** `POST /api/business-auth/login` (phone + password, BCrypt). Route: `/login`.

| ID       | Scenario                                                    | Steps                                                                                                                                         | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| RS-T-101 | Successful login (approved business)                        | Go to `/login`. Enter phone (`[data-testid="phone-input"]`) + password (`[data-testid="password-input"]`), click `[data-testid="login-btn"]`. | Redirected to `/dashboard`. Sidebar shows all nav items.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| RS-T-102 | Invalid credentials                                         | Enter a wrong password for a real account, submit.                                                                                            | `[data-testid="error-message"]` shows `restaurant.login.errorInvalidCredentials` (i18n key), not a raw error code. Stays on `/login`.                                                                                                                                                                                                                                                                                                                                                                              |
| RS-T-103 | Deactivated account                                         | Log in with a business-user account that has `isActive: false`.                                                                               | `[data-testid="error-message"]` shows the distinct `restaurant.login.errorDeactivated` message (triggered by backend `ACCOUNT_DEACTIVATED` error code, see `login.component.ts`), not the generic invalid-credentials one.                                                                                                                                                                                                                                                                                         |
| RS-T-104 | **Login routing ignores onboarding status — confirmed gap** | Log in with a `pending` or `rejected` account (not yet approved).                                                                             | Per `login.component.ts`'s own comment ("For now, route to dashboard"), you land on **`/dashboard`** regardless of status — not `/onboarding` or RS-003 as `RESTAURANT-TASKS.md`'s RS-001 acceptance criteria describes. There is no guard anywhere in `app.routes.ts` that redirects a non-approved business away from `/dashboard`. Confirm this reproduces and treat it as a real gap — a not-yet-approved partner can currently reach every screen in the app, including placing themselves as if operational. |
| RS-T-105 | Guest guard                                                 | While already logged in, manually navigate to `/login`, `/apply`, or `/accept-invite`.                                                        | `guestOnlyGuard` redirects you to `/dashboard` — you don't see the login/apply/invite forms again.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RS-T-106 | Auth guard on deep link                                     | Log out (or open an incognito tab), navigate directly to `/orders` or any authenticated route.                                                | `businessAuthGuard` redirects to `/login`, not shown the page or a raw error.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RS-T-107 | Session expiry mid-session                                  | Clear the stored Business JWT (or let it expire) while sitting on a page, then trigger any API call (reload).                                 | Kicked back to `/login`, not shown a raw 401/500 in the UI. (There's no dedicated 401-interceptor code path visible in the reviewed files — verify what actually happens; if it's a blank page or console error instead of a redirect, that's a gap to file.)                                                                                                                                                                                                                                                      |
| RS-T-108 | Logout                                                      | Click the sidebar logout button (bottom of nav, no `data-testid` — select by text or the `.logout-btn` class).                                | Returned to `/login`; deep-linking back to any authenticated route afterward redirects to `/login` again (token actually cleared).                                                                                                                                                                                                                                                                                                                                                                                 |

---

## 2. Partner Application — Public Signup (RS-002)

**Backend:** `POST /api/business-applications` (public, TASK-039). Route: `/apply`.

| ID       | Scenario                                                  | Steps                                                                                                                                                                                                                       | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-201 | Step 1 — business basics                                  | Go to `/apply`. Fill `[data-testid="apply-business-name"]` and `[data-testid="apply-business-type"]` (restaurant/grocery select), click Next.                                                                               | "Next" is disabled until name is filled; advances to step 2 (step indicator shows step 2 active). **Updated 2026-09-06 (onboarding gap-closing Phase 3):** when Type is `restaurant`, this step also shows `[data-testid="apply-restaurant-category"]` (QSR/Casual Dining/Cafe/Cloud Kitchen/Other), `[data-testid="apply-cuisine-types"]` (comma-separated text), `[data-testid="apply-open-time"]`/`[data-testid="apply-close-time"]` (`type="time"`), and `[data-testid="apply-is-24-hours"]` (checkbox) — none of these are required to advance. See RS-T-208 below for dedicated coverage. |
| RS-T-202 | Step 2 — address                                          | Fill `[data-testid="apply-address-town"]` and `[data-testid="apply-address-phone"]`, click Next.                                                                                                                            | **Note the scope:** this step only collects town + phone, not a full address (no house/street, pincode, or state field exists) — despite the spec calling for "address fields" plural. Advances to step 3 once both are filled.                                                                                                                                                                                                                                                                                                                                                                 |
| RS-T-203 | Step 3 — owner account & submit                           | Fill owner name (`[data-testid="apply-owner-name"]`), owner phone (**no `data-testid`** — use `#owner-phone`, a confirmed gap vs. the spec's `apply-owner-phone`), password (`[data-testid="apply-password"]`), click Next. | Next is disabled until all three fields are filled. **Updated 2026-09-06 (onboarding gap-closing Phase 1):** this no longer submits directly — it advances to a new step 4, the OTP gate (RS-T-210–213). The final `[data-testid="apply-submit-btn"]` now lives on step 4 and stays disabled until the phone is OTP-verified.                                                                                                                                                                                                                                                                   |
| RS-T-204 | **Success screen's Sign-In link is dead — confirmed bug** | From the success screen (now reached via step 4, RS-T-213), click "Sign In".                                                                                                                                                | **Does nothing.** `RestaurantApplyComponent`'s `imports` array is `[FormsModule, I18nPipe]` — `RouterLink` is not imported, even though the template uses `routerLink="/login"` on this link. No compile error (this is the exact silent-dead-link bug pattern that's shown up before in this codebase). Confirm it reproduces, then file it — the only way off this screen is a manual URL edit or browser back.                                                                                                                                                                               |
| RS-T-205 | Duplicate phone/business conflict                         | Submit the same phone number twice (full flow including OTP, see RS-T-210–213).                                                                                                                                             | Backend returns `CONFLICT`; UI should show `restaurant.apply.errorConflict`, not a generic error or a silent failure. Verify this actually surfaces — the component maps `CONFLICT` specifically, so anything else falls back to `common.error`.                                                                                                                                                                                                                                                                                                                                                |
| RS-T-206 | Back navigation preserves data                            | From step 2, 3, or 4, click "← Back".                                                                                                                                                                                       | Previously entered fields on the earlier step are still populated (form state isn't reset by stepping back).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| RS-T-207 | Submitted application visible to admin                    | After a successful submit, check `zitro-admin`'s pending-businesses list (AD-004-equivalent).                                                                                                                               | New application appears with `pending` onboarding status — cross-app check, requires `zitro-admin` running.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

**RS-T-208–217 below are new scenarios added 2026-09-06 for the onboarding gap-closing
Phases 1–4 (see `apps/tasks/RESTAURANT-ONBOARDING-GAP-ANALYSIS.md`). Live-tested and
verified against a local `zitro-api` + Postgres stack — results and 3 bugs found/fixed are
in the "Live Execution Log — 2026-09-06" section below.**

| ID       | Scenario                                          | Steps                                                                                                                                                                                                  | Expected                                                                                                                                                                                                                                                                                                                                                                            |
| -------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-208 | Restaurant category, cuisines, hours — happy path | With Type = `restaurant`, fill Name, pick a Restaurant Category, type comma-separated Cuisines (e.g. "Italian, Continental"), set Opening/Closing time, submit the full flow (step 1→4, OTP included). | On success, `GET /api/businesses/{id}` (or the admin detail view) shows `restaurantCategory`, `cuisineTypes` (as an array), `openTime`, `closeTime` all persisted exactly as entered.                                                                                                                                                                                               |
| RS-T-209 | Category/cuisine/hours fields hidden for grocery  | Switch Type to `grocery` on step 1.                                                                                                                                                                    | `apply-restaurant-category` and `apply-cuisine-types` inputs disappear (`@if (businessType==='restaurant')` gate in the template) — grocery applicants aren't asked for a cuisine. Verify open/close/24h fields' visibility too (same gate applies to all 4 new fields as a group).                                                                                                 |
| RS-T-210 | "Open 24 hours" checkbox                          | Check `[data-testid="apply-is-24-hours"]`.                                                                                                                                                             | Verify whether the Opening/Closing time inputs disable or hide when checked — read the current template to confirm the actual behavior before asserting it in an automated test (not independently re-verified in this pass beyond confirming the field renders and accepts input).                                                                                                 |
| RS-T-211 | OTP step renders after step 3                     | Complete steps 1–3 with valid data, click Next.                                                                                                                                                        | A 4th step renders: "We need to confirm this phone number belongs to you before submitting your application: {phone}", a `[data-testid="apply-otp-input"]`-adjacent "Send OTP" button, and a `Submit Application` button that is disabled until OTP is verified. **Verified live 2026-09-06.**                                                                                      |
| RS-T-212 | Send OTP                                          | Click "Send OTP".                                                                                                                                                                                      | `POST /api/auth/otp/request` fires with the owner's phone; UI swaps to an OTP-code input + "Verify OTP" + "Resend OTP". **Verified live 2026-09-06** — 200 OK, OTP session row created in `otp_sessions`.                                                                                                                                                                           |
| RS-T-213 | Verify OTP (dev bypass code)                      | Enter `123456` (the dev `Otp.BypassOtp` value — only works when `Otp.BypassForDevelopment=true`, i.e. local dev only) into the OTP input, click "Verify OTP".                                          | UI shows "✅ Phone number verified." and the OTP form disappears, leaving only Back/Submit Application. **Verified live 2026-09-06** — `POST /api/auth/otp/verify` returns 200. See the Live Execution Log below for a real bug found and fixed here (bypass mode didn't actually mark the session verified server-side, silently breaking every downstream consumer of that fact). |
| RS-T-214 | Submit after OTP verification                     | With OTP verified (RS-T-213), click "Submit Application".                                                                                                                                              | `POST /api/business-applications` succeeds (200), success screen renders. **Verified live 2026-09-06** after the bypass-marking fix — see Live Execution Log.                                                                                                                                                                                                                       |
| RS-T-215 | Submit blocked without OTP verification           | Reach step 4 but do NOT verify OTP (skip RS-T-212/213 entirely — not currently possible via the UI since Submit is disabled, but confirm the backend independently rejects a direct API call too).     | `POST /api/business-applications` for a phone with no recently-verified `OtpSession` returns `400 PHONE_NOT_VERIFIED`. **Verified live 2026-09-06** via direct API call (this is exactly the failure mode the bypass-marking bug produced for every submission before the fix).                                                                                                     |
| RS-T-216 | OTP verification validity window (30 min)         | Verify OTP, wait >30 minutes (or inspect `CreateBusinessApplicationHandler`'s `PhoneVerificationValidityMinutes` constant), then submit.                                                               | Submission should be rejected with `PHONE_NOT_VERIFIED` past the 30-minute window — not independently re-verified in this pass (would require a real 30-minute wait or clock manipulation); documented as the expected behavior per source, not live-confirmed.                                                                                                                     |
| RS-T-217 | Resend OTP                                        | On step 4, click "Resend OTP" after already receiving one.                                                                                                                                             | A new `POST /api/auth/otp/request` fires; the previous session's OTP should no longer verify (only the newest unused session is checked by `VerifyOtpHandler`). Not independently re-verified in this pass beyond confirming the button exists and is wired to the same `sendOtp()` method as the first send.                                                                       |

---

## 3. Accept Admin Invite (RS-002b)

**Backend:** `GET /api/business-invite/{token}`, `POST /api/business-invite/{token}/accept` (TASK-039). Route: `/accept-invite?token=...`.

| ID       | Scenario                                                                                | Steps                                                                                                                                                                                     | Expected                                                                                                                                                                                                                                             |
| -------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-301 | Valid token — business name shown                                                       | Navigate to `/accept-invite?token=<valid>`.                                                                                                                                               | Business name renders (plain text, **no `data-testid`** — a confirmed gap vs. the spec's `invite-business-name`). No password field testid either — use `#invite-pass`.                                                                              |
| RS-T-302 | Missing/invalid token                                                                   | Navigate to `/accept-invite` with no `token` query param, or an invalid one.                                                                                                              | An error message renders (`restaurant.invite.invalidToken`) — but **no `data-testid="invite-expired-message"`** exists (confirmed gap); select by text/class instead.                                                                                |
| RS-T-303 | Set password and accept                                                                 | With a valid token, enter a password, click the accept button (text "Set Password" / i18n key `restaurant.invite.setPassword` — **no `data-testid="invite-accept-btn"`**, confirmed gap). | Success screen shows with a "Sign In" link.                                                                                                                                                                                                          |
| RS-T-304 | **No confirm-password field — confirmed gap**                                           | Look for a second "confirm password" field before accepting.                                                                                                                              | **There isn't one** — the form is single-password only, despite the spec's `invite-confirm-password-input` testid. A typo'd password has no way to be caught client-side. Confirm and file as a gap, don't assume you missed it.                     |
| RS-T-305 | **Success screen's Sign-In link is dead — confirmed bug (same root cause as RS-T-204)** | From RS-T-303's success screen, click "Sign In".                                                                                                                                          | Does nothing — `RestaurantAcceptInviteComponent`'s `imports` array is also `[FormsModule, I18nPipe]`, missing `RouterLink`, while its template also uses `routerLink="/login"`. Same bug, second occurrence in the same file (`apply.component.ts`). |
| RS-T-306 | Token single-use                                                                        | After RS-T-303 succeeds, revisit the same invite URL.                                                                                                                                     | Should show "already used" — verify server-side rejection (not just a client no-op); the component doesn't have distinct copy for this vs. a generic invalid token, so check what actually renders.                                                  |
| RS-T-307 | Login works immediately after accept                                                    | Log in (RS-001) with the phone/password just set via the invite.                                                                                                                          | Succeeds — confirms the invite-accept flow actually activated the account server-side.                                                                                                                                                               |

---

## 4. Onboarding / KYC Completion (RS-003)

**Route:** `/onboarding` (auto-linked from login only if you navigate there manually — see
RS-T-104, nothing redirects you here automatically).

| ID       | Scenario                                                          | Steps                                                                                           | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-401 | **`/onboarding` is a stub, not a KYC form — confirmed major gap** | Navigate to `/onboarding` directly.                                                             | `RestaurantOnboardingComponent` is two lines of text: a heading and "Complete your profile and KYC... Go to Profile & Settings to upload your FSSAI, GST, and PAN documents" linking to `/profile`. **None** of the spec's KYC fields/testids exist anywhere in the app: no `kyc-status-banner`, no `kyc-pan`, no `kyc-bank-account`, no `kyc-doc-upload-*` (FSSAI/GST/PAN/bank document upload), no `kyc-save-draft-btn`, no `kyc-submit-btn`, no masked/reveal toggle for sensitive fields. What actually exists (via `/profile`, see §15) is 5 plain-text fields: name, description, phone, FSSAI number, GST number — no PAN, no bank/payout account, no document uploads at all. This is the single largest gap between spec and shipped code in the app. Confirm and file — RS-003 is marked `[x]` in `RESTAURANT-STATUS.md` as "status display + link to profile/KYC" which telegraphs the scope-down, but the actual gap is larger than that description suggests (there's no status banner testid, no PAN/bank fields anywhere, no doc upload anywhere). |
| RS-T-402 | Status text via `/profile`                                        | Log in as a `pending`/`rejected` account, go to `/profile`.                                     | A status banner (plain div, no `data-testid`) shows the raw `onboardingStatus` string and, if rejected, the rejection reason inline — but there's no distinct color/urgency styling described in the spec (yellow-pending/red-rejected) — verify what actually renders.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| RS-T-403 | Resubmit after rejection                                          | As a rejected account, edit the name/description/phone/FSSAI/GST fields on `/profile` and save. | Save succeeds (same endpoint as any other profile edit — there's no separate "resubmit" action, it's just another `PUT`). Confirms the only remediation path is editing whatever the 5 available fields are — PAN/bank/documents remain uneditable because they don't exist in the UI at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

---

## 5. Dashboard (RS-004)

**Backend:** `GET /api/business-portal/{businessId}/dashboard`. Route: `/dashboard`.

| ID       | Scenario                             | Steps                                                                                                                                                   | Expected                                                                                                                                                                                                                                                                                         |
| -------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| RS-T-501 | Stat cards load                      | Go to `/dashboard`.                                                                                                                                     | `[data-testid="dashboard-today-orders"]`, `[data-testid="dashboard-today-revenue"]` (₹-prefixed), `[data-testid="dashboard-pending-count"]`, `[data-testid="dashboard-stock-alert-count"]` all populate with real numbers.                                                                       |
| RS-T-502 | **No revenue chart — confirmed gap** | Look for a week's-revenue sparkline/chart.                                                                                                              | **There isn't one.** `BusinessDashboardDto` does include a `weekRevenue: number[]` array from the API, but `dashboard.component.ts`'s template never renders it — no `[data-testid="dashboard-revenue-chart"]` element exists at all. The data is fetched and silently unused. Confirm and file. |
| RS-T-503 | Quick links work                     | Click "View pending orders" and "Add menu item".                                                                                                        | Both use `routerLink` correctly (this component's `imports` array **does** include `RouterLink` — unlike the two bugs above) — "View pending orders" navigates to `/orders?status=pending`, "Add menu item" to `/menu`.                                                                          |
| RS-T-504 | Zero-state                           | Use a brand-new business account with no orders yet.                                                                                                    | Stat cards show `0`, not broken/undefined — no console errors.                                                                                                                                                                                                                                   |
| RS-T-505 | Stats match RS-005/RS-011            | Compare `dashboard-pending-count` and `dashboard-stock-alert-count` against the live values on `/orders?status=pending` and `/inventory`'s alerts list. | Numbers agree (same underlying data, different screens).                                                                                                                                                                                                                                         |

---

## 6. Live Orders Queue (RS-005)

**Backend:** `GET .../orders?status=`, `PUT .../orders/{orderId}/status`. Route: `/orders`.

| ID       | Scenario                          | Steps                                                                                                                                              | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-601 | Tab filtering                     | Go to `/orders`. Click each tab (`[data-testid="order-queue-tab-pending"]` etc. — 5 tabs: pending/confirmed/preparing/ready/shipped).              | List reloads filtered to that status; active tab is visually distinct.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| RS-T-602 | Order card renders                | Inspect a card, `[attr.data-testid]="'order-card-' + orderId"`.                                                                                    | Shows order ID, item count, total, time (`HH:mm`), status badge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| RS-T-603 | Accept a pending order            | On a `pending` order, click `[data-testid="order-accept-btn"]`.                                                                                    | Order advances to `confirmed`, disappears from the pending tab, list reloads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| RS-T-604 | Advance a non-pending order       | On a `confirmed`/`preparing`/`ready`/`shipped` order, click `[data-testid="order-advance-btn"]`.                                                   | Advances exactly one step per the fixed `STATUS_NEXT` map in `orders.component.ts` (confirmed→preparing→ready→shipped→delivered) — never an arbitrary jump.                                                                                                                                                                                                                                                                                                                                                                                                |
| RS-T-605 | Reject requires a reason          | Click `[data-testid="order-reject-btn"]` on a pending order, leave the reason blank, try to confirm.                                               | The confirm button in the reject panel (no dedicated testid on the confirm button itself — only the input `[data-testid="order-reject-reason-input"]` has one) is disabled while empty.                                                                                                                                                                                                                                                                                                                                                                    |
| RS-T-606 | Reject with reason                | Fill the reason, confirm.                                                                                                                          | Order status becomes `cancelled` with the reason stored as the status-change `note`; order leaves the pending tab.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| RS-T-607 | Terminal orders are read-only     | Filter/inspect a `delivered` or `cancelled` order (not directly reachable via the 5 displayed tabs — check via Order Detail or a direct API call). | No accept/reject/advance buttons render for these — matches `nextStatus()` returning `null` for terminal statuses.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| RS-T-608 | New order appears without refresh | Place a new order from `zitro-customer` while sitting on `/orders`.                                                                                | **Verify carefully:** the spec calls for a Firestore real-time listener or poll so new orders appear without a manual refresh. `orders.component.ts` as reviewed only calls `load()` on `ngOnInit` and on user actions (tab click, accept, reject) — there is no visible polling interval or Firestore subscription in this component. If a new order genuinely doesn't appear until you manually re-click a tab or reload, that's a gap worth confirming and filing (the countdown/auto-refresh acceptance criterion from RS-005 may not be implemented). |
| RS-T-609 | Auto-cancel countdown             | Leave a pending order sitting past ~15 minutes.                                                                                                    | Spec calls for `[data-testid="order-timeout-countdown"]`. **Not found in the reviewed template** — confirm it's genuinely absent (not just conditionally hidden) and file as a gap.                                                                                                                                                                                                                                                                                                                                                                        |
| RS-T-610 | Detail link                       | Click "Detail" on any order card.                                                                                                                  | Navigates to `/orders/{orderId}` — uses `[routerLink]` correctly (this component does import `RouterLink`).                                                                                                                                                                                                                                                                                                                                                                                                                                                |

---

## 7. Order Detail (RS-006)

**Backend:** `GET .../orders/{orderId}`. Route: `/orders/:orderId`.

| ID       | Scenario                                             | Steps                                                      | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------- | ---------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-701 | Items section                                        | Open an order, check `[data-testid="order-detail-items"]`. | Line items with quantity, name, variation name (if any), line total; special instructions shown per-item when present.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| RS-T-702 | **Charges section only shows Total — confirmed gap** | Check `[data-testid="order-detail-charges"]`.              | Only renders a single "Total" row. The frozen `OrderCharges` contract (root `CLAUDE.md` §"OrderCharges Structure") has `packagingCharges`, `platformFee`, `gst`, `deliveryCharge`, `couponDiscount` — each with calculated/applied/waived breakdowns — none of which are rendered, even though `BusinessOrderDetailDto.charges` is typed as `Record<string, unknown>` and presumably carries this data from the API. Confirm the API response actually contains the breakdown (check the Network tab) and that the UI is simply not displaying it — that's the gap, not a backend absence. File against RS-006's acceptance criterion "All charge line items match `OrderCharges` shape exactly." |
| RS-T-703 | Customer section                                     | Check `[data-testid="order-detail-customer"]`.             | Customer phone and payment method shown. Spec calls for the phone to be masked/partial (per RS-005's card-level masking language) — verify whether the full phone number renders unmasked here, since `order-detail-customer` shows `order()!.customerPhone` directly with no masking logic visible in the template.                                                                                                                                                                                                                                                                                                                                                                              |
| RS-T-704 | Timeline section                                     | Check `[data-testid="order-detail-timeline"]`.             | Every status-change entry renders in order with `HH:mm` timestamps and notes (e.g. the reject reason from §6) where present.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RS-T-705 | Back navigation                                      | Click "← Orders".                                          | Returns to `/orders` (this link's component does import `RouterLink` correctly).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

---

## 8. Menu Management — Category & Item CRUD (RS-007)

**Backend:** `GET/POST/PUT/DELETE .../categories`, `.../products`. Route: `/menu`.

| ID        | Scenario                                                                                  | Steps                                                                                                                                                                            | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-801  | "AI Menu Import" nav button                                                               | Go to `/menu` (on an `independent`-mode business), click the "AI Menu Import" button in the page header.                                                                         | **Fixed as of 2026-08-19 (was a confirmed dead-link bug).** `RestaurantMenuComponent`'s `imports` array now includes `RouterLink`, matching the template's `routerLink="/menu/import"` on this button — verified by inspecting the current source (`menu.component.ts` imports `RouterLink` from `@angular/router` and lists it in the `@Component` `imports` array). Confirm it still navigates correctly to `/menu/import` before signing off; this was previously the app's only in-app entry point to RS-008 and had zero compiler/lint signal for the missing import (same pattern as RS-T-204/RS-T-305).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| RS-T-802  | Category list & selection                                                                 | Check `[data-testid="category-list"]`, click a category.                                                                                                                         | Item table filters to that category's items; "All items" option (no dedicated testid) clears the filter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| RS-T-803  | Add category                                                                              | Click `[data-testid="category-add-btn"]`, fill name, save.                                                                                                                       | New category appears in the sidebar list immediately.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| RS-T-804  | **Nested `<button>` in category delete control — verify in browser**                      | Inspect the DOM around a category's "✕" delete button.                                                                                                                           | The source template nests a `<button class="btn-icon">✕</button>` **inside** the category-select `<button class="cat-item-btn">` — invalid HTML5 (buttons can't contain interactive content). Browsers auto-repair this by hoisting/reparenting the inner button, which can produce surprising click targets or event bubbling. Verify in devtools: does clicking ✕ reliably delete only that category without also triggering the parent's "select category" handler? `$event.stopPropagation()` is called in the handler, but confirm it actually prevents the parent click given the repaired DOM, don't assume the source order is what renders.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| RS-T-805  | Delete category confirmation                                                              | Click a category's ✕.                                                                                                                                                            | A native `confirm()` dialog appears ("Delete category "X"?") — not a custom modal; this matches the code (`window.confirm`) but is a lower-fidelity UX than a styled dialog, worth noting if a designer expected otherwise.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| RS-T-806  | Add item — required fields                                                                | Click `[data-testid="item-add-btn"]`, leave name blank.                                                                                                                          | `[data-testid="item-form-save-btn"]` stays disabled until name is filled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| RS-T-807  | Add item — full form                                                                      | Fill `[data-testid="item-form-name"]`, `[data-testid="item-form-price"]`, `[data-testid="item-form-category"]`, `[data-testid="item-form-food-type"]`, toggle Available, save.   | Item appears in the table with correct name/price/category and a ✓/✗ availability indicator.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| RS-T-808  | **No description, image, variants, or flags in the item form — confirmed gap**            | Look for a description field, image upload, variant rows (label/price/default), or spicy/recommended/bestseller/new flags in the add/edit item panel.                            | **None exist.** The form is exactly 4 fields (name, price, category, food type) plus an availability checkbox — no `item-form-variant-row` (spec's variant add/remove UI is entirely absent), no image upload, no description, no flag toggles. `MenuItemDto` does carry `description`, `imageUrl`, `isSpicy`, `isRecommended` fields from the API, so existing items with that data set (e.g. via direct API/seed) will simply have no way to view or edit those fields from this screen. File as a gap against RS-007's full acceptance criteria.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| RS-T-809  | Food type options limited to veg/non-veg                                                  | Open the food type dropdown (`[data-testid="item-form-food-type"]`).                                                                                                             | Only `veg`/`non-veg` options exist — the platform's `FoodType` enum (veg/nonVeg/egg/vegan per other apps' usage) isn't fully represented here. Minor gap, worth flagging if egg/vegan items are expected to be manageable from this screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| RS-T-810  | Edit item                                                                                 | Click Edit on an existing item, change price, save.                                                                                                                              | Table reflects new price after reload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| RS-T-811  | Delete item                                                                               | Click Delete on an item, confirm the native dialog.                                                                                                                              | Item removed from the table (optimistic local update on success, no reload needed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| RS-T-811b | **Independent-mode menu access before approval — real deadlock found & fixed 2026-09-06** | Log in with a brand-new `independent`-mode business that has never been approved (`onboardingStatus: pending`, `isActive: false`), go to `/menu`, try to add a category/product. | Before the fix: **every call 404'd with `BUSINESS_INACTIVE`.** `BusinessPortalCatalogController.ValidateBusinessAccessAsync` required `IsActive == true`, but `IsActive` only ever becomes `true` once `ApproveBusinessHandler` runs — and (per Phase 3's go-live gate) that handler itself now refuses to approve an independent-mode business with zero products. A brand-new applicant could never create the one product needed to pass its own approval gate — a hard deadlock with no way out via this screen. **Fixed** by allowing access here whenever the business hasn't been approved yet (`OnboardingStatus != Approved`), in addition to the normal `IsActive == true` case; a business that WAS approved and later deactivated still correctly stays locked out. Applies to `BusinessPortalMenuImportController` (AI import, §9) too, for the same reason. **Verified live 2026-09-06** end-to-end: created a category + product for a Pending business via direct API call, then Admin approval succeeded immediately after. 3 new integration tests added (`BusinessPortalCatalogControllerTests.cs`). |
| RS-T-812  | Shared-catalog (brand-level) menu — screen switch                                         | On a business with `menu_mode: shared`, go to `/menu`.                                                                                                                           | **Implemented as of 2026-08-21 (was a confirmed gap as of the 2026-08-19 pass).** `RestaurantMenuComponent` reads the business's `menuMode` on load (`checkingMode` signal, then `menuMode()`) and, when `'shared'`, renders `<app-restaurant-shared-menu />` (`SharedMenuComponent`) **instead of** the normal category/item CRUD screen entirely — a full screen swap, not a visual distinction layered on the same screen as originally speculated. An `independent`-mode business still gets the normal CRUD screen (RS-T-802–811) unchanged. Verified live 2026-08-21 against EFC Pizza (`menu_mode: shared`, brand `EFC Pizza Group`): the shared-menu table renders, not the category/item CRUD UI.                                                                                                                                                                                                                                                                                                                                                                                                              |
| RS-T-813  | Shared menu — table renders brand's master catalog                                        | On the shared-menu screen (RS-T-812), inspect `[data-testid="shared-menu-table"]`.                                                                                               | Table lists every product from the brand's master catalog (`GET` via `BusinessApiService.getBrandMasterProducts()`), showing Name, Master Price, an editable "Your Price" input (`[data-testid="shared-menu-price-{productId}"]`), a Hidden checkbox (`[data-testid="shared-menu-hidden-{productId}"]`), an Available checkbox (`[data-testid="shared-menu-available-{productId}"]`), and a per-row Save button (`[data-testid="shared-menu-save-{productId}"]`). No add/edit/delete controls for the products themselves — this screen can only layer branch-level exceptions on top, not touch the master catalog (that's an admin-level action).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| RS-T-814  | Shared menu — set a price override                                                        | Type a value into a row's "Your Price" input, click that row's Save.                                                                                                             | The override persists to `branch_item_overrides` (verify via DB/API: `price_override` set to the typed value, `is_hidden=false`, `is_available=true`). Row's placeholder still shows the master price for reference. Verified live 2026-08-21: overriding "7 Up" (master ₹45) to ₹50 correctly persisted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| RS-T-815  | Shared menu — hide / mark unavailable                                                     | Check a row's Hidden or uncheck Available, Save.                                                                                                                                 | Persists the same way as RS-T-814. A hidden row gets a dimmed (`.row-hidden`, `opacity: 0.5`) style in this table — confirm the item is actually excluded from the customer-facing menu for this branch, not just visually dimmed here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RS-T-816  | Shared menu — clearing an override reverts to default                                     | On a row with an existing override (from RS-T-814/815), reset price to blank, uncheck Hidden, check Available (i.e. back to the master defaults), Save.                          | The component deletes the override row entirely (`deleteBranchOverride`) rather than upserting a no-op — confirm via DB that the `branch_item_overrides` row for that product is gone, not left sitting with default-equivalent values.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RS-T-817  | Shared menu — empty master catalog                                                        | On a shared-mode business whose brand has no master products yet.                                                                                                                | `@empty` block renders a single-row "no master menu yet" message (`restaurant.noMasterMenu` i18n key) instead of an empty table or a crash.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

---

## 9. Menu Import — AI Photo/PDF Upload (RS-008)

**Backend:** `POST .../menu-import/parse`, `POST .../menu-import/commit`
(`BusinessPortalMenuImportController.cs`). Route: `/menu/import` (**only reachable by typing
the URL directly** until RS-T-801 above is fixed). Gated behind the `ai_menu_import` feature
flag — seeded **OFF**. When off, or when `Anthropic:ApiKey` isn't configured, the parse
endpoint returns `200 { enabled: false, message: "..." }` (not an error status) so the
frontend can show a graceful fallback instead of an error toast.

### 9a. Flag OFF (default state — test this first, no manual setup needed)

| ID       | Scenario             | Steps                                                                                                                                                                                                                                                                            | Expected                                                                                                                                                                                                                                                                                                                          |
| -------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-901 | Upload with flag off | Navigate directly to `/menu/import`. Select any file via the file input (no `data-testid` — spec's `menu-import-upload-zone` doesn't exist; it's a plain `<input type="file" multiple>`), click Parse (also untested — no `data-testid="menu-import-parse-btn"`, confirmed gap). | Parse request returns `enabled: false`; `featureDisabled` signal flips true; the info-card fallback renders: "🤖 [ai import disabled message]" + an "Add manually" button.                                                                                                                                                        |
| RS-T-902 | Fallback link works  | From RS-T-901's fallback state, click "Add manually".                                                                                                                                                                                                                            | **This one works correctly** — `RestaurantMenuImportComponent`'s `imports` array does include `RouterLink`, and it's used here on `routerLink="/menu"`. Navigates to `/menu` where the owner can use manual CRUD (§8). This is the one "graceful fallback" scenario from the task brief that's genuinely implemented as designed. |
| RS-T-903 | No file list shown   | Select 2–3 files.                                                                                                                                                                                                                                                                | The component tracks `files: File[]` internally but the template never renders a file list back to the user (no `[data-testid="menu-import-file-list"]`, confirmed gap) — you get no visual confirmation of what was selected beyond the browser's native file-input label.                                                       |

### 9b. Flag ON (requires the 3 manual setup steps in §0)

| ID       | Scenario                                                    | Steps                                                                                                                           | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-904 | Upload and parse                                            | With the flag on and API key configured, upload a real menu photo, click Parse.                                                 | `parseMenuImages` returns `{ enabled: true, categories: [...] }`; a flat read-only preview renders — category names as headings, items as `name — ₹price` lines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| RS-T-905 | **Review UI is flat/non-interactive — confirmed major gap** | Inspect the preview after a successful parse.                                                                                   | The spec describes a rich per-item review UI: editable name/price, food-type change, reorder, delete-a-row, per-item approve/reject, bulk "approve all"/"reject all"/"approve category" actions, low-confidence warnings, duplicate-name warnings against the existing catalog. **None of this exists.** The actual `menu-import.component.ts` renders each parsed category/item as static text with no edit controls, no per-item accept/reject, and a single global "Import All" button. None of the spec's testids exist: no `menu-import-review-category-{tempId}`, `menu-import-review-item-{tempId}`, `menu-import-approve-item-btn`, `menu-import-reject-item-btn`, `menu-import-approve-all-btn`, `menu-import-duplicate-warning`, or `menu-import-low-confidence-flag`. A misread name or price from the AI parse has **no in-UI way to be corrected** before commit — the owner's only options are "import everything as parsed" or abandon the whole import and go back to manual entry. This is the biggest scope gap in the app relative to its own task card; file prominently. |
| RS-T-906 | Commit ("Import All")                                       | Click "Import All" (`menu-import-commit-btn` testid from spec doesn't exist on this button — confirmed gap; select it by text). | Calls `commitMenuImport` with the full unedited parsed category list; on success, the preview clears. Since there's no partial-approval mechanism (RS-T-905), "commit" here always means "commit everything the AI returned, as returned."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| RS-T-907 | Abandon before commit                                       | After a successful parse, navigate away (e.g. click sidebar "Menu") without clicking Import All.                                | Nothing was persisted — confirm via `/menu` that no new categories/items appeared. This part of the acceptance criteria ("abandoning the flow at any point writes nothing") does hold, since commit is a distinct explicit action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| RS-T-908 | Post-commit items appear in RS-007                          | After RS-T-906, go to `/menu`.                                                                                                  | Imported categories/items appear and are editable through the normal manual-CRUD form from §8 (with the same field limitations noted in RS-T-808 — e.g. any variants the AI parsed have no UI to view/edit even post-commit, since the manual form doesn't support variants either).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| RS-T-909 | Low-confidence / blurry input                               | Upload a deliberately blurry or handwritten-menu photo.                                                                         | Per RS-T-905, there's no visible per-item confidence flag — verify whether the parse simply returns its best guess with no warning at all, which is worth flagging given the spec explicitly calls for surfacing low-confidence parses rather than silently guessing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| RS-T-910 | Re-import (duplicate detection)                             | Import the same menu photo twice in a row.                                                                                      | No duplicate-detection UI exists (RS-T-905) — expect the second import to create duplicate categories/items with no warning. Confirm and file, since RS-008's acceptance criteria explicitly calls for a duplicate-name warning here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

---

## 10. Menu Import — Bulk Spreadsheet (RS-009)

**Route:** not present in `app.routes.ts` as a distinct path — `RESTAURANT-STATUS.md` marks
this `[x]` with the caveat "pending bulk spreadsheet form (uses RS-008 commit endpoint)."

| ID        | Scenario                                            | Steps                                                   | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------- | --------------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1001 | **No dedicated bulk-import screen — confirmed gap** | Look for a route or nav entry for CSV/XLSX bulk import. | **None found.** There is no `/menu/bulk-import` (or similar) route in `app.routes.ts`, no component file for it, and none of the spec's testids (`bulk-import-template-btn`, `bulk-import-upload-zone`, `bulk-import-preview-table`, `bulk-import-row-error-{rowIndex}`, `bulk-import-commit-btn`) exist anywhere in the reviewed source. Despite `RESTAURANT-STATUS.md` marking RS-009 `[x]`, there is no separate spreadsheet-upload UI — only the AI photo/PDF flow from §9 exists as an "import" path. Treat RS-009 as **not actually testable as a distinct feature** until this is confirmed with the team; don't spend time hunting for a hidden entry point. |

---

## 11. Menu — Clone from Branch/Brand (RS-010)

**Route:** also not present in `app.routes.ts`.

| ID        | Scenario                                    | Steps                                                             | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------- | ------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1101 | **No clone-from-branch UI — confirmed gap** | Look for any control to clone a sibling branch's or brand's menu. | **Not found.** No route, no component, none of the spec's testids (`clone-source-select`, `clone-preview-list`, `clone-item-checkbox`, `clone-commit-btn`). `RESTAURANT-STATUS.md` calls RS-010 "done (placeholder)" — the placeholder appears to mean no UI exists yet, not a stub screen with a "coming soon" message. Only relevant for `menu_mode: shared` businesses, so this may be a low-priority gap depending on how many test businesses actually use shared mode — confirm the businesses in your test environment before treating this as urgent. |

---

## 12. Inventory Management (RS-011)

**Backend:** `GET .../inventory`, `POST .../inventory/adjust`, `GET .../inventory/alerts`. Route: `/inventory`.

| ID        | Scenario                                                         | Steps                                                                                                                                                                                             | Expected                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1201 | Table loads                                                      | Go to `/inventory`, check `[data-testid="inventory-table"]`.                                                                                                                                      | Product/qty/threshold rows; qty below threshold renders in the error color.                                                                                                                                                                                                                                                                                                                                       |
| RS-T-1202 | Adjust stock                                                     | Click `[data-testid="inventory-adjust-btn"]` on a row, enter a qty change (`[data-testid="inventory-adjust-qty-input"]`), pick a reason (`[data-testid="inventory-adjust-reason-select"]`), save. | Panel closes and the table reloads reflecting the new quantity.                                                                                                                                                                                                                                                                                                                                                   |
| RS-T-1203 | **Negative-stock error has no visible handling — confirmed gap** | Adjust a low-stock item by a large negative quantity so the backend should reject with `INSUFFICIENT_STOCK`.                                                                                      | `saveAdjust()` in `restaurant-features.component.ts` has no `error:` callback on its `subscribe()` — a rejected request fails **silently**: no error message, the panel doesn't close, no visible feedback at all. Confirm this reproduces (open devtools Network tab to see the actual 4xx) — it's a real gap against RS-011's "matches backend `INSUFFICIENT_STOCK` error, shown clearly" acceptance criterion. |
| RS-T-1204 | Alerts list                                                      | Check `[data-testid="inventory-alerts-list"]` (only renders when `alerts().length > 0`).                                                                                                          | Matches items genuinely at/below threshold — cross-check against RS-T-1201's table.                                                                                                                                                                                                                                                                                                                               |

---

## 13. Delivery Zones (RS-012)

**Backend:** full CRUD on `.../delivery-zones`. Route: `/delivery-zones`.

| ID        | Scenario                                                           | Steps                                                                                                 | Expected                                                                                                                                                                                                                                                                                                                                                |
| --------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1301 | Zone list                                                          | Go to `/delivery-zones`, check `[data-testid="zone-list"]`.                                           | Cards show name, active/inactive badge, base fee.                                                                                                                                                                                                                                                                                                       |
| RS-T-1302 | Add zone                                                           | Click `[data-testid="zone-add-btn"]`, fill name and fee, save via `[data-testid="zone-save-btn"]`.    | New zone card appears.                                                                                                                                                                                                                                                                                                                                  |
| RS-T-1303 | **`zone-fee-base` testid is on the wrong element — confirmed bug** | Try to select the base-fee **input** via `[data-testid="zone-fee-base"]` (e.g. in a Playwright spec). | It resolves to the `<label>` element, not the `<input>` — the source has `<label ... data-testid="zone-fee-base">Base fee (₹)</label>` immediately followed by the actual `<input>` with no testid of its own. Any test trying to type into this selector will fail (labels aren't fillable). File as a one-line fix (move the attribute to the input). |
| RS-T-1304 | **No per-km fee or surge multiplier fields — confirmed gap**       | Look for `zone-fee-per-km` and `zone-fee-surge` inputs in the add-zone form.                          | **Neither exists** — the form only has name + base fee. `BusinessZoneDto` itself only has `{id, name, baseFee, isActive}`, so this is a full-stack scope-down from the spec's "base fee, per-km fee, surge multiplier" — not just a missing UI control.                                                                                                 |
| RS-T-1305 | **No map or radius-based zone drawing — confirmed gap**            | Look for `zone-map-draw` or any boundary-drawing control.                                             | Not present. The spec explicitly allowed a simpler radius fallback if no map component was available — neither exists; zones currently have no geographic definition at all beyond a name and flat fee.                                                                                                                                                 |
| RS-T-1306 | No active/inactive toggle in the form                              | Try to create a zone as inactive from the add form.                                                   | No toggle exists in `showForm`'s panel — `isActive` defaults to `true` in the component's `f` object with no way to change it before save. (There's no edit action either — only add + delete.)                                                                                                                                                         |
| RS-T-1307 | Delete zone                                                        | Click `[data-testid="zone-delete-btn"]`, confirm the native dialog.                                   | Zone removed from the list.                                                                                                                                                                                                                                                                                                                             |

---

## 14. Ratings & Reviews (RS-013)

**Backend:** `GET .../ratings`, `POST .../ratings/{ratingId}/reply`. Route: `/ratings`.

| ID        | Scenario                                    | Steps                                                                                       | Expected                                                                                                                                                                                            |
| --------- | ------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1401 | Review list                                 | Go to `/ratings`, check `[data-testid="rating-list"]`.                                      | Star rating (★/☆ characters), comment, target type per review.                                                                                                                                      |
| RS-T-1402 | Reply to an unreplied review                | Fill `[data-testid="rating-reply-input"]`, click `[data-testid="rating-reply-submit-btn"]`. | Reply persists; that review now shows the reply read-only (📩 prefix) instead of the input form — matches acceptance criteria.                                                                      |
| RS-T-1403 | Already-replied review                      | Find a review with an existing reply.                                                       | Shows read-only reply, no input/submit rendered — can't double-reply. Confirmed correct in the template's `@if (r.reply)` branch.                                                                   |
| RS-T-1404 | No max-length enforcement on reply — verify | Try submitting a very long reply (500+ characters).                                         | No `maxlength` attribute visible on the reply `<input>` in the source — verify whether the backend rejects an overlong reply with a clear error, since the frontend won't stop you from typing one. |

---

## 15. Payouts (RS-014)

**Backend:** `GET .../payouts`. Route: `/payouts`.

| ID        | Scenario                                                       | Steps                                                                               | Expected                                                                                                                                                                                                                                                                                                                                                                                   |
| --------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| RS-T-1501 | Payout history table                                           | Go to `/payouts`.                                                                   | Table with period, gross, net, status per row — **the table itself has no `data-testid`** at all (spec's `payout-history-table`, confirmed gap); select via `.table` class or row text.                                                                                                                                                                                                    |
| RS-T-1502 | **No commission-rate or bank-account display — confirmed gap** | Look for the current commission rate and linked payout bank account on this screen. | **Neither exists.** No `[data-testid="payout-commission-rate"]`, no `[data-testid="payout-bank-account-display"]`, and no such fields anywhere in `RestaurantPayoutsComponent`'s template. This lines up with the missing bank-account fields noted in §4/§15 (RS-003/RS-015) — the data isn't collected anywhere in the UI in the first place, so there's nothing to display here either. |
| RS-T-1503 | Net = gross − commission (sanity check)                        | Compare a payout row's net vs. gross.                                               | Difference should be plausible for whatever commission rate the business is actually on — you can't independently verify the deducted rate from this UI (per RS-T-1502), so this is a "does it look sane" spot-check rather than a precise assertion.                                                                                                                                      |
| RS-T-1504 | Status styling                                                 | Compare a `pending` vs. `paid` (or similar) payout row.                             | Uses the shared `.badge-{status}` class — visually distinct, consistent with orders/zones badges elsewhere in the app.                                                                                                                                                                                                                                                                     |

---

## 16. Business Profile & Settings (RS-015)

**Backend:** `GET/PUT /api/business-portal/{businessId}`. Route: `/profile`.

| ID        | Scenario                                                                                  | Steps                                                                                                                                                                                                     | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1601 | **No `data-testid` on the core profile-edit fields — confirmed gap, narrowed 2026-09-06** | Try to select the name/description/phone/FSSAI/GST/PAN fields on `/profile` via `[data-testid="profile-name"]` etc.                                                                                       | **Still none resolve** for the original form fields (`#prof-name`, `#prof-desc`, `#prof-phone`, `#prof-fssai`, `#prof-gst`, `#prof-pan`, save button) — use `id` selectors for now. **Narrowed 2026-09-06:** this gap does NOT extend to the Phase 1–4 additions below (cover photo, KYC docs, payout, commission) — those all gained real `data-testid`s as part of this pass (see RS-T-1606–1615), since leaving newly-written code without them would repeat this exact codebase-wide anti-pattern for the very sections just added.                                                                                                                                                                                                                                                                                                                                   |
| RS-T-1602 | **Only 6 of the spec's ~9 core fields exist — confirmed gap, narrowed 2026-09-06**        | Compare the fields present (`name`, `description`, `phone`, `fssai`, `gst`, `pan`) against the spec (name, description, address, hours, min-order-amount, delivery fee, pure-veg flag, logo/cover image). | **Address, hours, min-order-amount, delivery-fee, and pure-veg flag are still absent.** **No longer accurate as originally written:** "logo/cover image" is no longer missing — cover photo upload now exists (RS-T-1607) as of Phase 3 of the onboarding gap-closing work, and `PanNumber` is also now a real editable field (was already present when this row was last touched, re-confirmed here). Cuisine type(s) and restaurant category are collected at apply-time (RS-T-208) but have **no edit UI on this screen** — the backend's `UpdateBusinessProfileRequest` accepts them (so the API contract isn't the limiting factor here, unlike the other still-missing fields), but nothing in `profile.component.ts` exposes them for editing after initial application. Worth a small follow-up if owners need to correct a mis-set cuisine/category post-signup. |
| RS-T-1603 | Save                                                                                      | Edit name/description/phone/FSSAI/GST, submit.                                                                                                                                                            | Success text (`common.saved`) appears; values persist on reload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| RS-T-1604 | Patch semantics                                                                           | Change only one field (e.g. phone), save, then check the actual PUT request body in devtools.                                                                                                             | Spec calls for sending only changed fields. `updateProfile()` in `business-api.service.ts` actually sends **all 5** form fields every time (`{ name, description, phone, fssaiLicenseNumber, gstNumber }`), not a diff — verify this against the acceptance criterion "Save only sends changed fields," it appears not to hold as written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| RS-T-1605 | No PAN/GST/bank masking                                                                   | Look for masked display + reveal toggle on the GST field (or any sensitive field).                                                                                                                        | Plain text input, always visible — matches the broader absence of masking noted in §4 (RS-003's acceptance criteria called for this).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

**RS-T-1606–1615 below are new scenarios added 2026-09-06 for the onboarding gap-closing
Phases 1–4** (cover photo, KYC document upload, read-only payout display, commission-terms
acceptance — all on this same `/profile` screen). Live-tested against a local stack; see the
"Live Execution Log — 2026-09-06" section below for full results and 3 bugs found/fixed.

| ID        | Scenario                                                  | Steps                                                                                                                                                        | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1606 | Cover photo — upload                                      | On `/profile`, use `[data-testid="profile-cover-photo-input"]` to select a jpeg/png/webp file.                                                               | `POST /api/business-portal/{businessId}/cover-photo` uploads to Firebase Storage under `businesses/{businessId}/cover/`, returns `{url}`; `[data-testid="profile-cover-photo-preview"]` (an `<img>`) appears/updates immediately without a page reload. **Verified live 2026-09-06** via direct API call — real Firebase Storage URL returned, `businesses.cover_image_url` persisted, and the image renders correctly after a page reload.        |
| RS-T-1607 | Cover photo — required-before-go-live hint text           | Read the copy under "Cover photo".                                                                                                                           | States "Required before your business can go live" — matches `ApproveBusinessHandler`'s actual gate (see admin-side `INCOMPLETE_LISTING` behavior, tested via `RESTAURANT-ONBOARDING-GAP-ANALYSIS.md`'s Phase 3 backend tests, not duplicated here since this is a `zitro-admin`-side check).                                                                                                                                                      |
| RS-T-1608 | KYC document upload — 4 slots                             | Use `[data-testid="profile-document-input-{pan\|fssai\|gst\|bank-proof}"]` to upload a file for each slot.                                                   | Each upload always lands as `status: "pending"` (owner can never self-verify); `[data-testid="profile-document-status-{type}"]` updates to show the badge immediately. **Verified live 2026-09-06** for the `pan` slot via direct API call — response `{"type":"pan","status":"pending",...}`, badge updates to "Pending" + a "View current file" link on reload.                                                                                  |
| RS-T-1609 | KYC document — re-upload replaces and resets to pending   | Upload a second file for a slot already showing `verified` or `rejected` (requires an Admin to have reviewed the first one — see `ADMIN-TEST-SCENARIOS.md`). | Status resets to `pending` — not independently re-verified in this pass (would require the admin-review round-trip first), documented as expected per `UploadBusinessDocumentHandler`'s upsert-by-type behavior.                                                                                                                                                                                                                                   |
| RS-T-1610 | Payout account — read-only display                        | Check `[data-testid="profile-payout-account"]` before and after Admin sets `PayoutAccountId` (`zitro-admin`, `ADMIN-TEST-SCENARIOS.md`).                     | Shows the pending-hint text until Admin sets a value, then shows the raw account identifier — always read-only here, matching Phase 2's design (owner uploads bank-proof evidence, only Admin turns it into a trusted value). Not independently re-verified end-to-end with a real admin-side write in this pass (Phase 2's own backend tests already cover the write path).                                                                       |
| RS-T-1611 | Commission terms — rate display                           | Check `[data-testid="profile-commission-rate"]`.                                                                                                             | Shows "Platform commission: {N}%" using `CommissionPercentage` (defaults to 0 if an admin has never set one — not `undefined`/blank). **Verified live 2026-09-06** — showed "Platform commission: 15%" for EFC Pizza.                                                                                                                                                                                                                              |
| RS-T-1612 | Commission terms — accept (not yet accepted)              | With `commissionAcceptedAt` null, click `[data-testid="profile-accept-commission-btn"]`.                                                                     | `POST /api/business-portal/{businessId}/accept-commission` returns 204; the button is replaced by `[data-testid="profile-commission-accepted-badge"]` ("accepted") without a page reload. **Verified live 2026-09-06** end-to-end via real UI click (not just API) — `commission_accepted_at` persisted server-side, UI updated in place.                                                                                                          |
| RS-T-1613 | Commission terms — already accepted                       | Reload `/profile` after RS-T-1612.                                                                                                                           | The "accepted" badge shows immediately on load (no button) — `commissionAcceptedAt` is non-null in the fetched profile. **Verified live 2026-09-06.**                                                                                                                                                                                                                                                                                              |
| RS-T-1614 | Commission terms — re-accept after Admin changes the rate | Have Admin change `CommissionPercentage` on this business (`zitro-admin`), then reload `/profile`.                                                           | `UpdateBusinessHandler` resets `CommissionAcceptedAt` to null whenever the rate actually changes — the badge should disappear and the Accept button reappear showing the new rate. Not independently re-verified through the `zitro-restaurant` UI in this pass (covered by a dedicated backend unit test, `UpdateBusinessHandlerTests.Handle_WhenCommissionPercentageChanges_ShouldResetCommissionAcceptedAt`); worth a follow-up UI-level check. |
| RS-T-1615 | Commission terms — go-live gate reflects acceptance       | With cover photo + ≥1 product done but commission NOT yet accepted, have Admin attempt to approve this business (`zitro-admin`).                             | `ApproveBusinessHandler` returns `INCOMPLETE_LISTING` naming "commission terms acceptance" as one of the missing items. **Verified live 2026-09-06** via direct admin API call — error message correctly listed all 3 missing items together before any were completed, and approval succeeded (200) once cover photo, ≥1 product, and commission acceptance were all done.                                                                        |

---

## 17. Staff Management (RS-016)

**Backend:** `GET/POST/PUT .../users`. Route: `/staff`.

| ID        | Scenario                                                      | Steps                                                                                                                                                                                      | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1701 | **No `data-testid` anywhere on this screen — confirmed gap**  | Try `[data-testid="staff-list"]`, `[data-testid="staff-add-btn"]`, `[data-testid="staff-role-select"]`, `[data-testid="staff-active-toggle"]`, `[data-testid="staff-reset-password-btn"]`. | **None exist** — `RestaurantStaffComponent`'s template has zero `data-testid` attributes. Select by table structure / button text for now.                                                                                                                                                                                                                                                                                                                                                                                                     |
| RS-T-1702 | Add staff                                                     | Click "+ Add Staff", fill name/phone/password, pick role (`manager` or `staff` — no `owner` option, correct per spec), save.                                                               | New row appears in the table with name/phone/role/active columns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| RS-T-1703 | **No edit/deactivate/reset-password actions — confirmed gap** | Look for any way to edit an existing staff member's role, toggle active status, or reset their password.                                                                                   | **None of this exists in the UI** — the table is add-only, display-only after that (no action buttons per row at all). `BusinessApiService.updateStaff()` exists and is fully wired for a PUT call, but nothing in `RestaurantStaffComponent`'s template ever calls it — the plumbing is half-built (service method present, no UI trigger). File as a gap against RS-016's "edit role/active status/reset password" acceptance criterion.                                                                                                     |
| RS-T-1704 | **No role-based route guard — confirmed gap**                 | Log in as a `staff`-role account, navigate to `/staff` directly.                                                                                                                           | Per RS-016's acceptance criteria, a `staff`-role user should be unable to reach this screen at all (route guard by role). `app.routes.ts` only applies `businessAuthGuard` (checks _authentication_, not _role_) to every child route including `/staff` — there is no role-based guard anywhere in the routing config. Confirm a `staff` account can currently load this page, and file it as a real permission gap, not just a hidden-button UX issue — RS-016 explicitly calls out that this needs to be a route guard, not hidden buttons. |
| RS-T-1705 | Manager cannot create owner                                   | As a `manager`-role account, check the role dropdown when adding staff.                                                                                                                    | Dropdown only offers `manager`/`staff` (hardcoded in the template, not role-conditional) — so this holds for every account, not just managers, which incidentally satisfies "owner role cannot be created from this UI" even without dedicated logic. Worth noting it's incidental, not purpose-built enforcement.                                                                                                                                                                                                                             |

---

## 18. Not Yet Testable / Known Gaps

Don't spend time hunting for these — they're either confirmed not built (per code review) or
explicitly out of scope per the status board:

| Item                                                                                   | Status                                    | Note                                                                                                                                                                                         |
| -------------------------------------------------------------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-017 — Push Notifications (Android FCM)                                              | `[ ]` not started                         | No FCM registration, no order-alert push, no notification tap-to-deep-link exists in the reviewed source. Also still needs a device-token registration endpoint check per its own task card. |
| RS-018 — Firebase Hosting Deploy (`partners.zitro.in`)                                 | `[ ]` not started                         | App is only reachable via local dev server — no custom-domain deploy yet.                                                                                                                    |
| RS-009 — Bulk Spreadsheet Import                                                       | Marked `[x]`, but no UI found             | See §10 — no route, component, or testids exist. Confirm with the team whether this was actually shipped somewhere not covered by this review, or whether the status board is stale.         |
| RS-010 — Clone from Branch/Brand                                                       | Marked `[x]` ("placeholder"), no UI found | See §11 — no route, component, or testids exist.                                                                                                                                             |
| RS-003 KYC form (PAN, bank account, document uploads, masking)                         | Confirmed gap                             | See §4/§16 — only 5 fields exist anywhere in the profile/onboarding surface; none of the document-upload or masking behavior exists.                                                         |
| RS-008 rich review UI (per-item edit/approve/reject, duplicate & low-confidence flags) | Confirmed gap                             | See §9b — only a flat preview + single "import all" exists.                                                                                                                                  |
| RS-012 map/radius zone drawing + per-km/surge fees                                     | Confirmed gap                             | See §13 — zones are name + flat base fee only.                                                                                                                                               |
| RS-016 role-based route guard + edit/deactivate/reset-password                         | Confirmed gap                             | See §17.                                                                                                                                                                                     |
| Automated test coverage                                                                | `[ ]` RS-TEST-001/002 not started         | Nothing here has a Vitest/Playwright spec yet — this document is the only test coverage that exists right now.                                                                               |

---

## 19. Cross-cutting

| ID        | Scenario                                                                                       | Steps                                                                                                                                                                                                       | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------- | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RS-T-1901 | **RouterLink import bug — run this scan explicitly, not just the 3 confirmed instances above** | For every component under `apps/apps/zitro-restaurant/src/app/features/` and `layout/`, cross-check `routerLink`/`[routerLink]` usage in the template against the `imports` array of the same `@Component`. | Confirmed broken (dead link, no navigation, no compile error) as of this review: `features/menu/menu.component.ts` (RS-T-801), and both components in `features/onboarding/apply.component.ts` (RS-T-204, RS-T-305). Confirmed **correctly** imported: `layout/restaurant-layout.component.ts`, `features/dashboard/dashboard.component.ts`, `features/menu/menu-import.component.ts`, `features/orders/orders.component.ts`, `features/orders/order-detail.component.ts`, `features/login/login.component.ts`, and the `RestaurantOnboardingComponent` inside `restaurant-features.component.ts`. Re-run this check after any of the fixes above land, and again before sign-off — this bug pattern has recurred multiple times in this codebase and produces zero compiler/lint signal. |
| RS-T-1902 | Permission boundary — role enforcement is inconsistent                                         | Compare what's actually enforced (route-guard-wise) for `owner` vs `manager` vs `staff` across every route in `app.routes.ts`.                                                                              | Only one guard exists app-wide (`businessAuthGuard`, authentication only) — there is **no role-differentiated guard anywhere**, not just on `/staff` (RS-T-1704). If `manager`/`staff` accounts are expected to have reduced access to e.g. `/payouts` or `/profile`, verify that's true — the routing config gives no indication it is.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| RS-T-1903 | Network failure resilience                                                                     | With devtools throttled to "offline," load each route in turn.                                                                                                                                              | Most components' `error:` callbacks only do `this.loading.set(false)` — meaning the page falls through to its `@else` "loaded" branch with empty arrays/null data rather than a distinct error state. Verify whether this reads as "no items yet" (misleading — looks like a legitimate empty state) vs. a genuine "failed to load" message. Several components (e.g. `restaurant-features.component.ts`'s inventory adjust-save, RS-T-1203) have no `error:` callback at all, so a failed write is completely silent.                                                                                                                                                                                                                                                                    |
| RS-T-1904 | Dual-mode: mobile viewport                                                                     | Resize to a mobile width (or run inside the Capacitor Android shell if available) and re-check the sidebar and any wide tables (menu item table, orders list, payouts table).                               | Sidebar collapses to a horizontal wrapped bar per its `@media (max-width: 768px)` rule (`layout/restaurant-layout.component.ts`). Wide tables (menu, staff, payouts) have no visible horizontal-scroll wrapper in the reviewed styles — verify they don't overflow the viewport awkwardly on a real phone width.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| RS-T-1905 | Direct API check alongside UI                                                                  | For any change made through the UI (order status, menu item, inventory adjust, zone, staff, profile), independently call the corresponding `GET` endpoint rather than trusting the UI's own re-render.      | Server state matches what the UI displays — this is especially worth doing given how many `error:` branches are missing or minimal (RS-T-1903): a save that silently failed client-side but you assume succeeded is exactly what this check catches.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RS-T-1906 | Business-scoped auth (TASK-031 boundary)                                                       | Using a Business JWT for business A, try to hit any `/api/business-portal/{businessB-id}/...` endpoint directly (devtools/Postman) with business B's ID.                                                    | Should be rejected — every `business-portal` route enforces `businessId == token.business_id` per the root `CLAUDE.md`'s note on TASK-031. This is a backend check, not a UI one, but worth an independent verification pass since so much of this app's own client-side guarding turned out to be thinner than expected (RS-T-1902).                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

---

**Summary of confirmed real bugs to prioritize** (all independently reproducible from source, listed once here for triage — full detail in the sections above):

1. **Login ignores `onboardingStatus`** (RS-T-104) — non-approved partners land on the full dashboard, not gated to onboarding. Still a confirmed gap as of 2026-08-21.
2. **Silent failures with no error UI**: several `error:` callbacks across the app still swallow failures (RS-T-1903) — inventory adjust itself (RS-T-1203) was fixed separately (see issue #16 in the log above).

**Fixed since this list was first written** (kept here so the numbering history in old notes/PRs
still resolves — see the Live Execution Log above for verification detail on each): dead "AI
Menu Import" nav button (RS-T-801, RouterLink import added), two dead "Sign In" links on the
apply/invite success screens (RS-T-204, RS-T-305, same RouterLink pattern), `zone-fee-base`
testid moved from a `<label>` onto its `<input>` (RS-T-1303).
