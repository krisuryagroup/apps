// Auth configuration models — re-exported from auth-config.model for backward compatibility.
export type { AuthSmsConfig, AuthUiConfig, AuthConfig } from './auth-config.model';
export { DEFAULT_AUTH_CONFIG } from './auth-config.model';
